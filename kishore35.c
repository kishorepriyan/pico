#include <stdio.h>

int main(void) {
	int i,j,count=0;
	char a[1000];
	printf("enter the sentence \n");
	scanf("%d %s",&j,a);
	for(i=0;a[i]!='\0';i++);
	{
		if(a[i]==j);
		count++;
	}
	printf("the numeric character is %d",count++);
	return 0;
}
