#include <stdio.h>

int main(void) {
	int a,n,t,d,sum=0;
	printf("enter the number");
	scanf("%d",&a);
	a=n;
	while(a!=0)
	{
		t=t+10+d;
	}
	if(sum==n)
	{
		printf("palindrome");
	}
	else
	{
		printf("not a palindrome");
	}
	return 0;
}
